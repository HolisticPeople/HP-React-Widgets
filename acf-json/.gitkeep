# Keep this folder in Git for ACF Local JSON sync
# When ACF field groups are saved in WP Admin, JSON files will be created here
# These files can then be committed and deployed to sync field groups across environments















